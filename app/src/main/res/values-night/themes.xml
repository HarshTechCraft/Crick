<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Try" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="tabSelectedBackgroundColor">@drawable/tab_selected_back_dark</item>
        <item name="android:statusBarColor">@color/black</item>

        <item name="tabSelectedTextColor">@color/white</item>
        <item name="bg">@color/black</item>
        <item name="blawhi">@color/black</item>
        <item name="aboutUs">@drawable/aboutus_dark</item>
        <item name="home">@drawable/home_dark</item>
        <item name="setting">@drawable/setting_dark</item>
        <item name="nav_item_background">@drawable/nav_item_background_dark</item>
        <item name="bgcolor">@drawable/border_dark</item>
        <item name="vs">@drawable/vs_dark</item>
        <item name="matchTabSelectedTextColor">@color/black</item>
        <item name="whibla">@color/black</item>
        <item name="vslive">@drawable/vsblack</item>
        <item name="line">@color/white</item>
        <item name="whigra">@color/gray</item>
        <item name="arrowDown">@drawable/down_white</item>
        <item name="arrowUp">@drawable/up_white</item>
        <item name="row_even">#23656565</item>
        <item name="row_odd">@color/gray</item>
        <item name="gradient">@drawable/gradient2</item>
        <item name="main_white">@color/white</item>


        <item name="android:textColor">@color/white</item>

    </style>
    <style name="TabTextAppearanceSelected" parent="">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">15sp</item>
    </style>
</resources>